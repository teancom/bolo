TODO

- collapse bolo core into:
  - a kernel thread
  - a scheduler thread
  - a supervisor thread
- adopt reactor pattern in core
- drop pthread_cleanup stuff in core

- debian package should create bolo user
- dump should probably dump counters and samples as well
- consistent hashing / dht algo
- find a good hash function - murmur?
- subscribers!
  - console event notifier
  - console grapher
  - bolo2rrd needs filtering abilities
  - package bolo2rrd RRA defs file
- memory usage analysis
- perf testing
- vim syntax rules:
  - bolo.conf
  - stat_bolo scripts
